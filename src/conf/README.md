# Legacy Config Notes

The maintained experiment configs now live under the repo-level `configs/` directory and are invoked through:

- `uv run icl-train`
- `uv run icl-paper ...`

This `src/conf/` tree is retained only as legacy reference material from the earlier project layout. Do not add new experiment definitions here.
